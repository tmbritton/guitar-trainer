# build artifacts
/guitar-trainer
/src/tools/audioprobe/audioprobe
/src/tools/sfcheck/sfcheck
/src/tsf/libtsf.a
*.o
*.obj

# soundfonts + cabinet IRs — third-party binaries; fetch with assets/fetch.sh
/assets/*.sf2
/assets/*.SF2
/assets/*.wav
/assets/*.WAV

# trial log (local data)
/trials.db

# per-machine audio-device selection (local)
/audio.txt

# imported songs' separated stems (local data; produced by assets/separate.py)
/library/

# generated screenshots (from --screenshot)
/gt_*.png

# scratch
/tmp/

# neural amp models (third-party; fetch with assets/fetch.sh)
/assets/*.nam
# NAM build artifacts + fetched deps (Eigen, namcore) — rebuilt by src/nam/build.sh
/src/nam/libnam.a
/src/nam/vendor/
/src/nam/obj/
/src/tools/namcheck/namcheck
/riff.wav

# SoundTouch build artifacts + fetched source — rebuilt by src/soundtouch/build.sh
/src/soundtouch/libsoundtouch.a
/src/soundtouch/vendor/
/src/soundtouch/obj/
